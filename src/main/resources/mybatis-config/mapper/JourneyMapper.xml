<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.repository.JourneyMapper">

    <select id="journeyList" resultType="com.example.entity.journey.JourneyDTO">
        select
        from
        order by
    </select>

    <insert id="registerJourney" parameterType="com.example.entity.journey.JourneyDTO">
        insert into
        values
    </insert>

    <select id="findByName" resultType="com.example.entity.journey.JourneyDTO" parameterType="int">
        select
        from
        where
    </select>

    <update id="updateJourneyMove" parameterType="com.example.journey.JourneyMoveDTO">
        update
        set
        where
    </update>

    <update id="updateJourneyAccommodation" parameterType="com.example.journey.JourneyAccommodationDTO">
        update
        set
        where
    </update>

    <update id="updateJourneyVisit" parameterType="com.example.journey.JourneyVisitDTO">
        update
        set
        where
    </update>

    <delete id="deleteJourney">

    </delete>

    <!--
    <select id="userLogin"
            resultType="com.example.entityUserDTO"
            parameterType="com.example.entityUserDTO">
        select * from usertbl
        where username=#{username} and password=#{password}
    </select>
    -->
    <delete id="bookDelete" parameterType="int">
        delete from book where num=#{num}
    </delete>
</mapper>

